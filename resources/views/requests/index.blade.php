@extends('layouts.app') @section('content')<div class="d-flex justify-content-between mb-3"><h1 class="page-title h3">{{ __('app.requests.title') }}</h1><a class="btn btn-primary" href="{{ route('requests.create') }}">{{ __('app.requests.new') }}</a></div><div class="card p-3"><form class="row mb-3"><div class="col-md-4"><select class="form-select" name="status" onchange="this.form.submit()"><option value="">{{ __('app.requests.all_statuses') }}</option>@foreach($statuses as $key=>$label)<option value="{{ $key }}" @selected(request('status')===$key)>{{ $label }}</option>@endforeach</select></div></form><div class="table-responsive"><table class="table"><thead><tr><th>{{ __('app.requests.reference') }}</th><th>{{ __('app.requests.citizen') }}</th><th>{{ __('app.requests.type') }}</th><th>{{ __('app.requests.service') }}</th><th>{{ __('app.common.state') }}</th><th></th></tr></thead><tbody>@forelse($requests as $item)<tr><td>{{ $item->reference }}</td><td>{{ $item->citizen->full_name }}</td><td>{{ $item->type }}</td><td>{{ $item->service->name }}</td><td><span class="badge text-bg-secondary">{{ $statuses[$item->status] }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('requests.show',$item) }}">{{ __('app.common.view') }}</a> <a class="btn btn-sm btn-outline-secondary" href="{{ route('requests.edit',$item) }}">{{ __('app.common.edit') }}</a></td></tr>@empty<tr><td colspan="6">{{ __('app.requests.none') }}</td></tr>@endforelse</tbody></table></div>{{ $requests->links() }}</div>@endsection
